import React from 'react';
import TestRenderer from 'react-test-renderer';
import TwitterMentions from './index';

// it('showiasdfasdfially', () => {
//   const testRenderer = TestRenderer.create(<TwitterMentions />);
//   const testInstance = testRenderer.root;
//   const divInstance = testInstance.findByProps({ id: 'mentions' });
//   expect(divInstance.props.children).toBe('0');
// });
//
// it('clicking - asdfasdfasdf', () => {
//   const testRenderer = TestRenderer.create(<TwitterMentions />);
//   const testInstance = testRenderer.root;
//   const buttonInstance = testInstance.findByProps({ children: '-' });
//   buttonInstance.props.onClick();
//   const divInstance = testInstance.findByProps({ id: 'mentions' });
//   expect(divInstance.props.children).toBe('-1');
// });
//
// it('clicking asdfasdfasdf+ increments', () => {
//   const testRenderer = TestRenderer.create(<TwitterMentions />);
//   const testInstance = testRenderer.root;
//   const buttonInstance = testInstance.findByProps({ children: '+' });
//   buttonInstance.props.onClick();
//   const divInstance = testInstance.findByProps({ id: 'mentions' });
//   expect(divInstance.props.children).toBe('1');
// });
